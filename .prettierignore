# Ignore generated files
CHANGELOG.md
pnpm-lock.yaml

# Generated by packages/react/scripts/buildIconTypes.js during the build.
# It is gitignored in packages/react/.gitignore, but Prettier (run from the
# repo root) does not read nested .gitignore files, so it is listed here too.
packages/react/src/components/Icon/types.ts

# VPAT reports are generated by the vpat-report workflow; Prettier would only
# re-pad their markdown tables, and enforcing it would fail the auto-generated
# VPAT PRs.
vpats/
