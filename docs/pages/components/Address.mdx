---
title: Address
description: A utility component to identify contact information or a physical address.
source: https://github.com/dequelabs/cauldron/tree/develop/packages/react/src/components/Address/index.tsx
---

import {
  Address,
  AddressLine,
  AddressCityStateZip
} from '@deque/cauldron-react';

```jsx
import {
import {
  Address,
  AddressLine,
  AddressCityStateZip
} from '@deque/cauldron-react'
```

## Examples

### Full Address

```jsx example
<Address>
  <AddressLine>1234 Sesame Street</AddressLine>
  <AddressCityStateZip city="Metrocity" state="AA" zip="8675309" />
</Address>
```

### Without State

```jsx example
<Address>
  <AddressLine>1234 Sesame Street</AddressLine>
  <AddressCityStateZip city="Metrocity" zip="8675309" />
</Address>
```

### Without Zip Code

```jsx example
<Address>
  <AddressLine>1234 Sesame Street</AddressLine>
  <AddressCityStateZip city="Metrocity" state="AA" />
</Address>
```

## Props

### Address

<ComponentProps children={true} className={true} />

### AddressLine

<ComponentProps children={true} className={true} />

### AddressCityStateZip

<ComponentProps
  children={true}
  className={true}
  props={[
    {
      name: 'city',
      type: 'string',
      description:
        'The city to be combined with state and zip in a single line.'
    },
    {
      name: 'state',
      type: 'string',
      description:
        'The state to be combined with city and zip in a single line.'
    },
    {
      name: 'zip',
      type: 'string',
      description:
        'The zip to be combined with city and state in a single line.'
    }
  ]}
/>
