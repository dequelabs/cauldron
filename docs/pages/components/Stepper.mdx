---
title: Stepper
description: A segmented progress bar.
source: https://github.com/dequelabs/cauldron/tree/develop/packages/react/src/components/Stepper/index.tsx
---

import { Stepper, Step } from '@deque/cauldron-react'

```jsx
import { Stepper, Step } from '@deque/cauldron-react'
```

Stepper is a segmented progress list that displays the current state of a multi-stepped process marking the current active step with `aria-current="step"`. This component is commonly used in wizard-like workflows.

## Examples

### Steps with Status

Each step can have a visible descriptive label describing each numerical step.

```jsx example
<Stepper>
  <Step status="current">
    Select all stuff/things/such
  </Step>
  <Step status="future">
    Analyze the stuff
  </Step>
  <Step status="future">
    Review all of the things, stuff, such and etc.
  </Step>
</Stepper>
```

### Steps with Tooltips

For more compact steppers, the additional descriptive information can be included via a tooltip. Both `tooltip` and `tooltipText` _must_ be included to ensure that each step has an accessible name.

```jsx example
<Stepper>
  <Step
    status="complete"
    tooltip={<><div>Step 1: Foo</div><div>Status: Complete</div></>}
    tooltipText="Foo Complete"
  />
  <Step
    status="current"
    tooltip={<><div>Step 2: Bar</div><div>Status: Current</div></>}
    tooltipText="Bar Current"
  />
</Stepper>
```

## Props

### Stepper

<ComponentProps
  children={true}
  className={true}
/>

### Step

<ComponentProps
  children={{
    description: 'children to be rendered as the step\'s visible label (if not visible label is desired, use the "tooltip" prop instead)'
  }}
  className={true}
  props={[
    {
      name: 'status',
      type: ['current', 'complete', 'future'],
      required: true,
      description: 'Current status of the step.'
    },
    {
      name: 'tooltip',
      type: 'React.ReactNode',
      description: 'Tooltip content to be rendered with the tooltip tabstop on the step (no visible label will be rendered)'
    },
    {
      name: 'tooltipText',
      type: 'string',
      description: 'Text summary of the tooltip, used as the Step\’s aria-label'
    }
  ]}
/>
