/**
 * Maps toast types to their classNames and icons
 */
export declare const typeMap: {
  confirmation: {
    className: string;
    icon: string;
  };
  caution: {
    className: string;
    icon: string;
  };
  'action-needed': {
    className: string;
    icon: string;
  };
};
export declare const tabIndexHandler: (
  reset: boolean,
  toast: HTMLElement | null
) => any;
