name: 'Dependencies'
description: 'Sets up Node and pnpm and installs dependencies for Cauldron'
inputs:
  install:
    description: 'Run pnpm install'
    default: 'true'

runs:
  using: 'composite'
  steps:
    - uses: pnpm/action-setup@d15e628ca66d93ee5f352c71671a7bc6a97af5c9 # v6.0.8
      # No version: — the action reads it from packageManager in package.json.
    - uses: actions/setup-node@v6
      with:
        node-version-file: '.nvmrc'
        cache: pnpm
        cache-dependency-path: 'pnpm-lock.yaml'
        registry-url: 'https://registry.npmjs.org'
    # OIDC trusted publishing in release.yml delegates to npm, which needs
    # 11.5.1+ for that. Pin to 11.11.0 to work around
    # https://github.com/npm/cli/issues/9151 in the bundled Node 22 npm.
    - run: npm install -g npm@11.11.0
      shell: bash
    - name: Install dependencies
      run: pnpm install --frozen-lockfile
      shell: bash
      # Note: Checking for both boolean and string true values due to referenced bug:
      # https://github.com/actions/runner/issues/2238
      if: ${{ inputs.install == true || inputs.install == 'true' }}
