name: Tests

on:
  pull_request:
    branches:
      - develop

jobs:

  react:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/dependencies
        with:
          packages-react: true
          packages-styles: true
      - run: yarn test

  a11y:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/dependencies
        with:
          packages-react: true
          packages-styles: true
      - run: yarn build:docs
      - run: yarn test:a11y
